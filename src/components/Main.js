import Projects from './Projects'

// the component function
const Main = (props) => {

    return (
    <div className="main-container"> 
        <Projects/>
    </div>
    )
}

// export the component
export default Main
